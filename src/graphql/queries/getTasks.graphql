query Tasks($input: FilterTaskInput!) {
  tasks(input: $input) {
    id
    name
    dueDate
    status
    pointEstimate
    tags
    assignee {
      id
      avatar
      fullName
      email
    }
  }
}
